@component
class Demo { #value = 0xFFn; }
const ratio = a / b / c;
const re = /[/]/gu;
const text = `hello ${ {value: `nested ${42}`} } done`;
const node = <div title="a&b">don't break <span>{true ? "yes" : "no"}</span></div>;
const other = <><A /><B data-id={42} /></>;
const f = <T,>(x: T) => x;
type Config = { readonly size: number };
const config = { size: 10 } satisfies Config;
